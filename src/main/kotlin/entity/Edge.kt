package entity

/**
 * Enum class representing the edges on a tile.
 * Each edge is identified by a numeric value from 0 to 5.
 */

enum class Edge {
    ZERO,
    ONE,
    TWO,
    THREE,
    FOUR,
    FIVE
}
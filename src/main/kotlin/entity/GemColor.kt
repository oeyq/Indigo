package entity

/**
 * Enumeration of gem colors.
 *
 * This enum class represents different colors that a gem can have. Each color is uniquely
 * identified by a constant value, and the possible colors are [AMBER], [EMERALD], and [SAPPHIRE].
 */
enum class GemColor {
    AMBER,
    EMERALD,
    SAPPHIRE
}